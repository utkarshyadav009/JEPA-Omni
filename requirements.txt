# Core
torch>=2.4
torchvision>=0.19         # required by the SigLIP2 AutoImageProcessor
transformers>=4.49        # SigLIP2 (loaded via AutoModel: vision_model / text_model towers)
torchcodec>=0.1           # video decoding (requires a matching FFmpeg install)
PyYAML>=6.0

# Image/text processing
Pillow>=9.0
sentencepiece>=0.1.99     # SigLIP2 tokenizer (Gemma/SentencePiece based)
